# Contracts

Este cambio no modifica contratos de API.

- Worker API: sin cambios (referencia canónica: `docs/worker-api.md`).
- Persistencia: sin cambios (referencia canónica: `docs/storage.md`).

El alcance es exclusivamente frontend (menú + Service Worker / flujo de actualización).
